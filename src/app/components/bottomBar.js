import style from "./comp.module.css"

export default function BottomBar() {
  return (
    <section
      id="bottomBar"
      className={`${style['container']} ${style['bottomBar']}`}
    >

    </section>
  )
}