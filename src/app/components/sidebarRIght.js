import style from "./comp.module.css"

export default function SidebarRight() {
  return (
    <section
      id="sidebar-right"
      className={`${style['container']} ${style['sidebar-right']}`}
    >

    </section>
  )
}